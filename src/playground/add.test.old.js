/*const add = (a,b) => a + b;

const generateGreeting = (name) => `Hello ${name}.`;

//tests always follow this format.
//test("description of the test goes here, in quotes", arrow function for test case)

test('should add two numbers', () => {
    const result = add( 3, 4);

    //Hard-coded test case:
    //if (result !== 7) {
    //    throw new Error(`You added 4 and 3. Expected 7. Returned ${result} `);
    //}

    //Jest pre-built test case:
    expect(result).toBe(7);
})

test('should return the string Hello /Name/', () => {
    const namio = generateGreeting('Britney');
    expect(namio).toBe("Hello Britney.")
})*/